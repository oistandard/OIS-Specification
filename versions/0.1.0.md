# Open In-Store Standard (OIS) 0.1.0 (Draft)

This draft defines the initial structure of the OIS specification layers.
Each layer includes JSON payload examples in `examples/`.

## Status

Draft. Not yet approved by the TSC.

## Conformance

OIS-compliant implementations MUST follow the normative requirements in this
specification and MAY include optional extensions as described by each layer.

## OIS-Core

Defines shared conventions across all layers.

* Authentication and authorization
* Canonical identifiers
* Timestamp, localization, and versioning

### Discovery

Providers SHOULD expose a public discovery document at
`/.well-known/ois` under their OIS endpoint. This document describes
supported OIS layers, authentication requirements, and transport security
so clients can configure access before making authenticated requests.

Example payloads:

* `examples/ois-core/authentication.json`

Schema:

* `schemas/ois-core/authentication.schema.json`

Schema:

* `schemas/ois-core/authentication.schema.json`

## OIS-Display

Screen and inventory definition layer.

Example payloads:

* `examples/ois-display/screen-inventory.json`

Schema:

* `schemas/ois-display/screen-inventory.schema.json`

## OIS-Media

Media lifecycle and delivery layer.

Example payloads:

* `examples/ois-media/media-delivery.json`

Schema:

* `schemas/ois-media/media-delivery.schema.json`

## OIS-Proof-of-Play

Measurement and compliance layer.

Example payloads:

* `examples/ois-proof-of-play/playback-event.json`

Schema:

* `schemas/ois-proof-of-play/playback-event.schema.json`

## OIS-Sense

Sensor and engagement layer.

Example payloads:

* `examples/ois-sense/sensor-event.json`

Schema:

* `schemas/ois-sense/sensor-event.schema.json`

## OIS-Location

Physical environment and location intelligence layer.

Example payloads:

* `examples/ois-location/store-layout.json`

Schema:

* `schemas/ois-location/store-layout.schema.json`

## OIS-Events

Real-time signaling and interaction layer.

Example payloads:

* `examples/ois-events/realtime-event.json`

Schema:

* `schemas/ois-events/realtime-event.schema.json`

## OIS-Commerce

Media-to-transaction bridge layer.

Example payloads:

* `examples/ois-commerce/transaction-attribution_OPT_1.json`
* `examples/ois-commerce/transaction-attribution_OPT_2.json`

Schema:

* `schemas/ois-commerce/transaction-attribution-opt-1.schema.json`
* `schemas/ois-commerce/transaction-attribution-opt-2.schema.json`
